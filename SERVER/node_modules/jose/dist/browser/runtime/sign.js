import subtleAlgorithm from 'jose/dist/browser/runtime/subtle_dsa.js';
import crypto from 'jose/dist/browser/runtime/webcrypto.js';
import checkKeyLength from 'jose/dist/browser/runtime/check_key_length.js';
import getSignKey from 'jose/dist/browser/runtime/get_sign_verify_key.js';
const sign = async (alg, key, data) => {
    const cryptoKey = await getSignKey(alg, key, 'sign');
    checkKeyLength(alg, cryptoKey);
    const signature = await crypto.subtle.sign(subtleAlgorithm(alg, cryptoKey.algorithm), cryptoKey, data);
    return new Uint8Array(signature);
};
export default sign;
